import setWordsControls from './controls-words';
import setControlsForPanel from './controls-for-panel';

function activeControls():void {
  setWordsControls();
  setControlsForPanel();
}

export default activeControls;
